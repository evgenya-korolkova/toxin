mixin dropdown(label="", attr={}, style={})

    //- - var guests = [{text: "взрослые", value: "0"}, {text: "дети", value: "0"}, {text: "младенцы", value: "0"}]
    - var guests = {'взрослые': '2', 'дети': '2', 'младенцы': '0'}

    //- - var rooms = [{text: "спальни", value: "2"}, {text: "кровати", value: "2"}, {text: "ванные комнаты", value: "0"}]
    - var rooms = [{text: "спальни", value: "2"}, {text: "кровати", value: "2"}, {text: "ванные комнаты", value: "0"}]

    //- - var data = []
    - var data = guests

    .dropdown(data-type=attr.type data-values=data)
        label.dropdown__label
            if label
                span.h3= label 
                br
            button.dropdown__input.body= value

        .dropdown__dropdown.none(tabindex="0")
            .dropdown__list
                //- each item in data
                each value, key in data
                    .dropdown__list-item
                        //- .dropdown__list-item-text.h3= item.text
                        .dropdown__list-item-text.h3= key
                        .dropdown__list-item-buttons
                            button.dropdown__list-item-button(data-key=key) -
                            //- .dropdown__list-item-value.h3= item.value
                            .dropdown__list-item-value.h3(data-key=key)= value
                            button.dropdown__list-item-button(data-key=key) +

            if attr.type === "guests"
                .dropdown__buttons
                    button.dropdown__button-clean.h3(autofocus="true") очистить
                    button.dropdown__button-apply.h3 применить

    //- - value = "Сколько гостей"


    //- if attr.type === "guests"
    //-     - data = guests
    //- else if attr.type === "rooms"
    //-     - data = rooms    

    //- - count = 0
    //- //- заполнить значение attr.value
    //- each item in guests
    //-     - count = count + +item.value

    //- if count
    //-     - value = count + " гостей" 
    //- else      
    //-     - value = "Сколько гостей"